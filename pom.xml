<project>
    <!-- model version - always 4.0.0 for Maven 2.x POMs -->
    <modelVersion>4.0.0</modelVersion>

    <!-- project coordinates - values which uniquely identify this project -->
    <groupId>staleChecker.app</groupId>
    <artifactId>staleChecker</artifactId>
    <version>0.1</version>

    <description>A ETL pipeline for news "staleness" checking</description>
    <url>https://github.com/haoyang09/staleChecker</url>


    <!-- library dependencies -->
    <dependencies>
        <dependency>
            <groupId>com.aamend.spark</groupId>
            <artifactId>spark-gdelt</artifactId>
            <version>x.y.z</version>
        </dependency>
    </dependencies>
    
    <dependency>
        <groupId>org.apache.kafka</groupId>
        <artifactId>kafka-streams</artifactId>
        <version>1.0.2</version>
    </dependency>
    <dependency>
        <groupId>org.apache.kafka</groupId>
        <artifactId>kafka-clients</artifactId>
        <version>1.0.2</version>
    </dependency>

</project>
